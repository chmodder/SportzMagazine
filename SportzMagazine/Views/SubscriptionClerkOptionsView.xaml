<Page
    x:Class="SportzMagazine.Views.SubscriptionClerkOptionsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SportzMagazine.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel  Grid.Row="0">
            <TextBlock Text="Subcription Clerk" HorizontalAlignment="Center" Style="{ThemeResource TitleTextBlockStyle}"/>
            <TextBlock Text="Options" HorizontalAlignment="Center" Style="{StaticResource SubtitleTextBlockStyle}"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <Button Content="Unapproved Subscriptions" Click="Button_Click" />
        </StackPanel>
    </Grid>
</Page>
